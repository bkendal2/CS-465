{{> header}}

<h1>{{title}}</h1>

<div class="row">
  {{#each trips}}
    <div class="col-md-4">
      <div class="card mb-4">
        <a href="/api/trips/{{code}}">
           <img src="/images/{{image}}" alt="{{name}}">
        </a>
        <div class="card-body">
          <h5>{{name}}</h5>
          <p>{{resort}}</p>
          <p>{{description}}</p>
        </div>
      </div>
    </div>
  {{/each}}
</div>

{{> footer}}